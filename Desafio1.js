// Como podemos rodar isso em um arquivo .ts sem causar erros? 
let employee = {};
employee.code = 10;
employee.name = "John";

// Resposta do desafio1
let funcionario = {codigo: Number, nome: String} ={
    codigo: 10,
    nome: "John",
}
console.log (`Codigo ${funcionario.code} e o nome ${funcionario.name} \n`);